package com.admin.ezpark.enums

enum class DashboardFields {
    AddOwner,
    RemoveOwner,
    UpdateOwnerInfo,
    ViewOwner,
}

enum class DashboardType(val type: Int) {
    TYPE1(1),
    TYPE2(2)
}